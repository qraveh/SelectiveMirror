{
  "name": "smirror-telemetry-worker",
  "version": "0.1.0",
  "private": true,
  "description": "Cloudflare Worker that proxies telemetry POSTs to Supabase, adding rate limiting and edge defenses.",
  "main": "src/index.ts",
  "scripts": {
    "dev":    "wrangler dev",
    "deploy": "wrangler deploy",
    "tail":   "wrangler tail"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20260101.0",
    "typescript":                "^5.5.0",
    "wrangler":                  "^3.80.0"
  }
}
