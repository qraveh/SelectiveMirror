# system-validation/allowlist.txt
#
# Tests in this file are tolerated as RED at release time by BOTH
# .github/workflows/release.yml and .github/workflows/release-dryrun.yml.
# This file is the single source of truth — both workflows read it
# at the system-validation step. PR-PRE-M1 (pre-release status panel
# 2026-05-03) made this consolidation: the prior hand-typed `$allowed`
# arrays in each workflow had drifted (release.yml empty; dryrun
# tolerated TestPanelR3_*, which had since been re-asserted to pass).
#
# Authority for adding an entry: each test name listed below must also
# appear in CHANGELOG.md under a `### Bugs known at tag` (or
# `### Known issues`) section, with rationale. The companion linter
# scripts/check-allowlist-vs-changelog.ps1 enforces "every entry here
# is mentioned in CHANGELOG" before the system-validation step runs.
#
# Format:
#   * One test name per line (e.g. TestPanelR4_CLI_FreshConfig_FileMode).
#   * No paths, no quotes — just the bare top-level Go test function.
#   * Lines starting with `#` are comments.
#   * Blank lines ignored.
#   * Leading / trailing whitespace stripped.
#
# Currently empty as of v1.0.0 tag-day (2026-05-03):
#   BUG-R3-1   closed by decision — test flipped to assert documented
#              divergence; now passes (CHANGELOG [1.0.0] line 98).
#   FIND-R4-1  closed by hooks deferral — test skipped, not failing
#              (CHANGELOG [1.0.0] "Deferred from v1.0 — pre/post-sync hooks").
#   BUG-R4-1   closed by implementation (CHANGELOG line 95).
#   BUG-R5-1   closed by implementation (CHANGELOG line 96).
#   NEW-R10-1  closed by implementation (CHANGELOG line 97).
#   OBS-R4-1   non-blocking (uses t.Logf only; CHANGELOG line 65).
#   R4-PF-10   no associated test (CHANGELOG line 66).
#
# Re-add an entry only with a matching CHANGELOG bullet that references
# the test name verbatim.

# v1.0.0 tag-day discovery (run 25304915932 against sha 9a97ace).
# CI-only failure: passes locally on Windows (3-of-3 reruns), fails
# on windows-latest runner. The test is observation-only (t.Logf, not
# t.Errorf/t.Fatalf) — its presence in the failure list suggests a
# panic or t.Helper t.Fatalf inside countSmirrorFilterTempFiles or
# the smirror sub-process spawning under runner-specific conditions
# (possibly contention on os.TempDir() with parallel tests, or AV
# interference with rapid Process.Kill cycles). The original "PANEL
# OBS: temp files leaked" intent doesn't gate the release; allowlist
# while v1.0.x investigates root cause.
TestPanelR2_Filter_TempFileLeakOnKill

# v1.0.0 dryrun #5 (run 25305574101 against sha 9fc1593) — same flake
# class as TempFileLeakOnKill: subprocess + filesystem-event timing
# under CI runner contention. Test passes locally on Windows but fails
# on windows-latest CI. Uses t.Errorf at panel_findings_round2_test.go:298;
# real failure mode is "watcher didn't see rename within 20s" which is
# a timing assertion sensitive to runner load. v1.0.x investigation
# direction: either widen the timeout, retry-on-flake at suite level,
# or reduce -parallel to limit contention.
TestPanelR2_Daemon_LiveSync_DirectoryRename

# v1.0.0 dryrun #8 + #9 (sha 683d198, 299bf9d) — same test failed
# under both parallel=4 (default) and parallel=2. Test creates many
# files at once and asserts the watcher queue handles depth gracefully
# within 30s. CI runner perf delta vs developer machine puts the
# assertion timeout at the edge. Not a parallelism issue (didn't
# improve when reduced) — pure runner-throughput. v1.0.x: widen the
# timeout, OR rewrite to assert-eventually-with-progress, OR move to
# the SLA-smoke workflow which has a longer wall-clock budget.
TestPanelR3_Queue_HighDepthGraceful
