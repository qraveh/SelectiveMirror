package systemval

// Coverage reporting is handled in TestMain (main_test.go) after m.Run()
// completes, so it sees all goals recorded by parallel tests.
//
// No test function needed here — the report prints automatically.
